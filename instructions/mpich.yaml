buildsi:
  release: 1 # Increment every time you'd like to trigger a build.
spack:
  specs: [mpich]
  versions:
    3.1.4:
      variants:
        - +device=ch3
        - +netmod=tcp
    3.0.4:
      variants:
        - +device=ch3
        - +netmod=tcp
